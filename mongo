# 使用官方 MongoDB 镜像
FROM mongo:7.0.5-jammy

# 设置环境变量
ENV MONGO_INITDB_ROOT_USERNAME=horde
ENV MONGO_INITDB_ROOT_PASSWORD=dbPass123

# 禁用 MongoDB 的日志输出
CMD ["--quiet", "--logpath", "/dev/null"]

# 映射端口
EXPOSE 27017

# 创建数据卷
VOLUME /data/db
